#include <stdio.h>
#include <ctype.h>
#include <string.h>
#include <cs50.h>

int main(void)
{
    char C;

    string S = get_string("S:");
    string S0 = get_string("S0:");

    printf("C:");
    scanf("%c", &C);

    int match = 0;

    for (int i = 0; i < strlen(S); i++)
    {
        
        if (C == S[i])
        {
            match++;
        }
    }

    int size = 5000;
    char newstring[size];

     for (int i = 0; i < strlen(S); i++)
     {
        if (C == S[i])
        {
            for (int j=0; j < strlen(S0); j++) 
	    {
                newstring[strlen(newstring)] = S0[j];
            }
        }

        newString[strlen(newstring)] = S[i];
    }

    printf("%s\n", newstring);
}